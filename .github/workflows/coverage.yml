name: Coverage Check

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [main, develop]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  coverage:
    name: Test Coverage
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: ${{ github.workspace }}
    env:
      PYTHONPATH: ${{ github.workspace }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'
          cache: 'pip'

      - name: Restore virtualenv cache
        id: cache-venv
        uses: actions/cache@v5
        with:
          path: .venv
          key: venv-${{ runner.os }}-py3.14-${{ hashFiles('requirements.txt', '.github/workflows/coverage.yml') }}

      - name: Install dependencies
        if: steps.cache-venv.outputs.cache-hit != 'true'
        run: |
          sudo apt-get install -y libjpeg-dev
          python -m venv .venv
          . .venv/bin/activate
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov pytest-xdist

      - name: Run tests with coverage
        run: .venv/bin/python -m pytest --cov --cov-report=term-missing:skip-covered --cov-fail-under=97

      - name: Generate coverage report
        if: always()
        run: .venv/bin/python -m coverage report

      - name: Upload coverage to artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: coverage-report
          path: htmlcov/
          retention-days: 7

      - name: Comment coverage on PR
        if: github.event_name == 'pull_request' && always() && github.event.pull_request.head.repo.full_name == github.repository
        uses: actions/github-script@v8
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            try {
              const output = require('child_process').execSync('.venv/bin/python -m coverage report', { encoding: 'utf-8' });
              const lines = output.split('\n');
              const totalLine = lines.find((line) => line.trim().startsWith('TOTAL'));
              if (!totalLine) {
                console.log('Could not find TOTAL line in coverage report');
                return;
              }

              const percentMatch = totalLine.match(/(\d+)%\s*$/);
              if (!percentMatch) {
                console.log('Could not parse coverage percentage from TOTAL line:', totalLine);
                return;
              }

              const total = Number(percentMatch[1]);
              const comment = `## Coverage Report\n\n**Total Coverage:** ${total.toFixed(2)}%\n\n${total >= 97 ? '✅ Meets 97% threshold' : '❌ Below 97% threshold'}`;
              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: comment
              });
            } catch (e) {
              console.log('Could not generate coverage PR comment:', e.message);
            }

  test:
    name: Run Functional Tests
    needs: coverage
    if: ${{ needs.coverage.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: ${{ github.workspace }}
    env:
      PYTHONPATH: ${{ github.workspace }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'
          cache: 'pip'

      - name: Restore virtualenv cache
        id: cache-venv
        uses: actions/cache@v5
        with:
          path: .venv
          key: venv-${{ runner.os }}-py3.14-${{ hashFiles('requirements.txt', '.github/workflows/coverage.yml') }}

      - name: Install dependencies
        if: steps.cache-venv.outputs.cache-hit != 'true'
        run: |
          python -m venv .venv
          . .venv/bin/activate
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-xdist

      - name: Run unit tests
        run: .venv/bin/python -m pytest -p no:cov -o addopts='' tests/ -v --tb=short -x

      - name: Run CLI integration tests
        run: .venv/bin/python -m pytest -p no:cov -o addopts='' tests/test_rune_cli_integration.py -v --tb=short

      - name: Run API tests
        run: .venv/bin/python -m pytest -p no:cov -o addopts='' tests/test_api_*.py -v --tb=short

      - name: Run model manager tests
        run: .venv/bin/python -m pytest -p no:cov -o addopts='' tests/test_ollama_model_manager.py -v --tb=short

      - name: Run Vast.ai integration tests (mocked)
        run: .venv/bin/python -m pytest -p no:cov -o addopts='' tests/test_vastai_instance_manager.py tests/test_vastai_mocked.py -v --tb=short

      - name: Run error path tests
        run: .venv/bin/python -m pytest -p no:cov -o addopts='' tests/test_comprehensive_error_paths.py -v --tb=short

      - name: Run edge case tests
        run: .venv/bin/python -m pytest -p no:cov -o addopts='' tests/test_edge_cases_micro_branches.py -v --tb=short

      - name: Generate test report
        if: always()
        run: |
          .venv/bin/python -m pytest -p no:cov -o addopts='' tests/ --tb=short -v --junit-xml=test-results.xml 2>&1 || true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-results
          path: test-results.xml
          retention-days: 7

      - name: Report test status
        if: always()
        run: |
          if [ "${{ job.status }}" = "success" ]; then
            echo "✅ All functional tests passed"
          else
            echo "❌ Functional tests failed (see the first failed pytest step above)"
          fi

  quality:
    name: Lint and Type Checks
    needs: coverage
    if: ${{ needs.coverage.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: ${{ github.workspace }}
    env:
      PYTHONPATH: ${{ github.workspace }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'
          cache: 'pip'

      - name: Restore virtualenv cache
        id: cache-venv
        uses: actions/cache@v5
        with:
          path: .venv
          key: venv-${{ runner.os }}-py3.14-${{ hashFiles('requirements.txt', '.github/workflows/coverage.yml') }}

      - name: Install dependencies
        if: steps.cache-venv.outputs.cache-hit != 'true'
        run: |
          python -m venv .venv
          . .venv/bin/activate
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install quality tools
        run: .venv/bin/pip install ruff mypy

      - name: Run ruff (syntax/runtime focused)
        run: .venv/bin/python -m ruff check --select=E9,F63,F7,F82 rune rune_bench tests

      - name: Run mypy
        continue-on-error: true
        run: .venv/bin/python -m mypy rune rune_bench --ignore-missing-imports --follow-imports=silent --no-error-summary

  security:
    name: Security Scans
    needs: coverage
    if: ${{ needs.coverage.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: ${{ github.workspace }}
    env:
      PYTHONPATH: ${{ github.workspace }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'
          cache: 'pip'

      - name: Restore virtualenv cache
        id: cache-venv
        uses: actions/cache@v5
        with:
          path: .venv
          key: venv-${{ runner.os }}-py3.14-${{ hashFiles('requirements.txt', '.github/workflows/coverage.yml') }}

      - name: Install dependencies
        if: steps.cache-venv.outputs.cache-hit != 'true'
        run: |
          python -m venv .venv
          . .venv/bin/activate
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install security tools
        run: .venv/bin/pip install bandit pip-audit

      - name: Run bandit on application code
        continue-on-error: true
        run: .venv/bin/python -m bandit -q -r rune rune_bench -f txt

      - name: Run dependency audit (advisory)
        continue-on-error: true
        run: .venv/bin/python -m pip_audit

  smoke:
    name: CLI and API Smoke Test
    needs: coverage
    if: ${{ needs.coverage.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: ${{ github.workspace }}
    env:
      PYTHONPATH: ${{ github.workspace }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'
          cache: 'pip'

      - name: Restore virtualenv cache
        id: cache-venv
        uses: actions/cache@v5
        with:
          path: .venv
          key: venv-${{ runner.os }}-py3.14-${{ hashFiles('requirements.txt', '.github/workflows/coverage.yml') }}

      - name: Install dependencies
        if: steps.cache-venv.outputs.cache-hit != 'true'
        run: |
          python -m venv .venv
          . .venv/bin/activate
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: CLI help smoke
        run: .venv/bin/python -m rune --help

      - name: API module smoke
        run: .venv/bin/python -c "from rune_bench.api_server import RuneApiApplication; print('ok')"

  docker-build:
    name: Docker Build Check
    needs: [test, quality, smoke]
    if: ${{ needs.test.result == 'success' && needs.quality.result == 'success' && needs.smoke.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: ${{ github.workspace }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build Docker image (no push)
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          push: false
          tags: ai-benchmarks:rune-ci
          cache-from: type=gha
          cache-to: type=gha,mode=max
